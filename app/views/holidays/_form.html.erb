<%= form_with(model: holiday, local: true) do |form| %>
  <% if holiday.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(holiday.errors.count, "error") %> prohibited this holiday from being saved:</h2>

      <ul>
      <% holiday.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :place %>
    <%= form.text_field :place, id: :holiday_place %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author, id: :holiday_author %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
